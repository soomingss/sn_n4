import{NextResponse}from"next/server";import{getCurrentSession,supabaseAdminFetch}from"../../lib/auth";export async function PATCH(req){const s=await getCurrentSession();if(!s)return NextResponse.json({error:"로그인이 필요합니다."},{status:401});const b=await req.json().catch(()=>({}));const payload={};for(const k of ["company_name","contact_name","phone","email"])if(typeof b[k]==="string")payload[k]=b[k].trim();const r=await supabaseAdminFetch(`/rest/v1/profiles?id=eq.${encodeURIComponent(s.user.id)}`,{method:"PATCH",headers:{Prefer:"return=representation"},body:JSON.stringify(payload)});if(!r.ok)return NextResponse.json({error:"회원정보 수정에 실패했습니다.",detail:await r.text()},{status:500});return NextResponse.json({ok:true});}
