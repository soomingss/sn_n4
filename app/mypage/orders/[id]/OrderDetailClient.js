"use client";import{useState}from"react";import{useRouter}from"next/navigation";export default function C({order}){const[msg,setMsg]=useState("");const router=useRouter();const cancel=async()=>{if(!confirm("이 주문을 취소하시겠습니까?"))return;const r=await fetch("/api/orders",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:order.id,action:"cancel"})});const d=await r.json();if(!r.ok){setMsg(d.error||"취소 실패");return}router.push("/mypage");router.refresh()};return <div className="customerOrderActions">{order.status==="new"&&<><a href={`/products?edit=${order.id}`}>주문 수정</a><button onClick={cancel}>주문 취소</button></>}<a href={`/products?reorder=${order.id}`}>이 주문 다시 담기</a>{msg&&<p>{msg}</p>}</div>}
