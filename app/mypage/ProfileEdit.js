"use client";import{useState}from"react";export default function ProfileEdit({profile}){const[open,setOpen]=useState(false),[form,setForm]=useState({company_name:profile?.company_name||"",contact_name:profile?.contact_name||"",phone:profile?.phone||"",email:profile?.email||""}),[msg,setMsg]=useState("");const save=async()=>{const r=await fetch("/api/profile",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const d=await r.json();setMsg(r.ok?"회원정보를 수정했습니다.":d.error||"수정에 실패했습니다.");if(r.ok)setTimeout(()=>location.reload(),500)};return <div className="profileEditBox"><button className="adminLinkButton" onClick={()=>setOpen(v=>!v)}>회원정보 수정</button>{open&&<div className="profileEditForm">{[["상호명","company_name"],["담당자명","contact_name"],["연락처","phone"],["이메일","email"]].map(([l,k])=><label key={k}><span>{l}</span><input value={form[k]} onChange={e=>setForm(v=>({...v,[k]:e.target.value}))}/></label>)}<p>아이디 · 승인상태는 관리자만 변경할 수 있습니다.</p>{msg&&<b>{msg}</b>}<button onClick={save}>저장</button></div>}</div>}
