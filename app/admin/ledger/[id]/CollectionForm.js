"use client";import{useState}from"react";export default function F({userId}){const[amount,setAmount]=useState(""),[method,setMethod]=useState("bank_transfer"),[memo,setMemo]=useState(""),[msg,setMsg]=useState("");const save=async()=>{const r=await fetch("/api/admin/ledger",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({user_id:userId,amount,payment_method:method,memo})});const d=await r.json();if(!r.ok){setMsg(d.error+(d.detail?` (${d.detail})`:""));return}location.reload()};return <div className="collectionForm"><b>수금 등록</b><input type="number" min="1" placeholder="수금액" value={amount} onChange={e=>setAmount(e.target.value)}/><select value={method} onChange={e=>setMethod(e.target.value)}><option value="card">카드</option><option value="bank_transfer">계좌이체</option><option value="cash">현금</option><option value="other">기타</option></select><input placeholder="메모 (선택)" value={memo} onChange={e=>setMemo(e.target.value)}/><button onClick={save}>수금 등록</button>{msg&&<span>{msg}</span>}</div>}
